# 更新日志

`0.x` 阶段接口可能在小版本之间变；数据库迁移只增不减，升级镜像即可，不用手工改库。

## [0.2.0] — TBD

- **导入账单**：支持支付宝 CSV（GBK）、微信 XLSX、通用模板，也能粘贴短信、通知的文字。预览时会猜类别，
  重复的自动跳过，可以逐行或批量修改。接口是 `POST /import/preview` 和
  `POST /transactions/bulk`，服务端零依赖解析 xlsx。
- **资产**：物品按「(买价 − 卖价) ÷ 用了几天」算日均成本，有在用、闲置、退役、卖出四种状态；
  基金、股票持仓通过腾讯行情接口自动更新价格，也能手填。买卖时可以顺手记一笔流水，默认勾上，
  靠 `clientId` 保证幂等。持仓市值计入净资产。迁移 `002`–`004`。
- **网页版**：≥ 840 dp 时账单页换成宽表格，支持多选批量改类别、基金，批量删除，
  快捷键 `N`、`/`、`Esc`、`Delete`。导入和资产管理也能在网页上做。
- **自动记账 AI 兜底**：可以选本地或远程渠道；触发方式分自动、手动，按本地日志里的命中率校准。
- **修复**：
  - 网页版随机卡死：首页停在骨架屏、账单表格出不来、登录和导入一直转圈。原因是 service worker
    换版本时，Chrome 会把 fetch 的 keepalive 请求扣着不发；现在网页端请求一律不带 keepalive。
  - Docker 用 bind mount 挂一个还不存在的宿主目录时，容器报 `unable to open database file` 反复重启。
    现在容器以 root 入场，先把 `/data` 的属主改成 1000（可用 `PUID` / `PGID` 改）再降权运行，不用手动 chown。
  - 局域网、Tailscale 地址自动补全端口。
  - 基金页的新建按钮和右下角的 + 重叠。
- **文档**：README 精简并加了截图，`docs/ios.md` 也压短了。

## 0.1.0 — 2026-09-13

首个可用版本。

- **服务端**：Node 24 单进程，零第三方依赖，数据存 `node:sqlite`。模块自动装载，
  迁移向前兼容。
- **账本**：账户 × 基金两层模型；支出、收入、转账、拨款；类别、成员、预算。按 `clientId`
  幂等提交，用全局 `seq` 做增量同步。金额一律用整数「分」存。
- **App**：Flutter 3.32 一份代码出 Android、iOS、网页三端。窄屏用底部导航，≥ 840 dp
  换成导航轨加双栏。
- **自动记账**：通知文本依次经过归一化、查重、按来源抽字段、用户规则，再用朴素贝叶斯猜类别和基金，
  低于置信度阈值的进「待确认」。Android 监听通知后在通知栏给出「正确 / 修改… / 撤销」；
  iOS 走分享扩展、快捷指令、剪贴板，原生代码还没编译验证过。
- **备份**：`VACUUM INTO` 快照，gzip 压缩后可选再加密，上传到 WebDAV，按份数保留；
  恢复前要先过 sha256 和 `integrity_check` 校验。
- **AI**：兼容 `anthropic`、`openai` 两种协议，内置八个预设；有月报和问答。密钥加密存在服务端，
  按成员限流。
- **部署与 CI**：单镜像，可以从 GHCR 拉，也可以本地构建。`test.yml` 在 push 时自动跑，
  `build.yml` 手动触发发布（多架构镜像、APK、GitHub Release）。
- **安全**：口令用 scrypt 存；令牌可吊销；登录和初始化都有限流；首次初始化可以用 `SETUP_TOKEN`
  加锁。`secret.key` 是所有加密的根，要和数据库一起备份。
