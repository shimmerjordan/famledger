# 更新日志

`0.x` 阶段接口可能在小版本之间变；数据库迁移只增不减，升级镜像即可，不用手工改库。

## [0.2.0] — TBD

- **导入账单**：支持支付宝 CSV（GBK）、微信 XLSX、通用模板，也能粘贴短信、通知的文字。预览时会猜类别，
  重复的自动跳过，可以逐行或批量修改。接口是 `POST /import/preview` 和
  `POST /transactions/bulk`，服务端零依赖解析 xlsx。
- **资产**：物品按「(买价 − 卖价) ÷ 用了几天」算日均成本，有在用、闲置、退役、卖出四种状态；
  基金、股票持仓通过腾讯行情接口自动更新价格，也能手填。买卖时可以顺手记一笔流水，默认勾上，
  靠 `clientId` 保证幂等。持仓市值计入净资产。迁移 `002`–`004`。
- **实物估值**：物品有了「现在值多少」。数码、出行、箱包每年打折，家电、家具、衣物、运动匀速折旧，
  首饰不折旧；可以套预设（苹果设备、新能源车、相机镜头、轻奢箱包、保值款……）、改年折率和残值，
  或者手动估一个数，之后从那天起算。新增「箱包/奢侈品」「首饰/贵金属」两个类别。卖掉、退役的估值归零，
  详情页给出处置盈亏（只展示，不记账）。服务端和 App 各算一份，共用一套黄金向量对数。
- **净资产总览**：资产页顶上第一次显示净资产（账户 · 投资 · 实物）。实物默认按类别计入（数码、出行、
  箱包/奢侈品、首饰），单件可以改；全局开关在总览里，只有管理员能改。`GET /stats/overview` 多了
  `physical` 和 `netWorthExPhysicalCents`，`/settings` 多了 `assets` 段。迁移 `005`（顺带补上持仓的 seq 索引）。
- **会员权益**：资产页多了「会员权益」tab（`/assets?tab=perks`）：平台 → 会员/卡 → 权益三层。每项权益能写在哪个平台领、
  领取路径和链接、额度（每月 N 次、每年 N 次、会籍期内 1 次、一次性、不限次，还能叠加「每年 6 次且每月最多 2 次」）、
  有效期、面值和限制条件；N 选 1 用「父权益 + 选项」表示；由别的卡的权益带出来的会员（88VIP 送的优酷会员）记下来源。
  「全部」视图可以按会员或按领取平台看，填了领取链接的能直接打开；权益可以归档（收进会员详情底部的「已归档」）；
  平台管理能改名、加别名、合并重复的平台。虚拟资产不计入净资产。
  接口 `/platforms`（含 `POST /platforms/:id/merge`）、`/memberships`、`/benefits`，删除带 `?cascade=1` 级联；
  409 的错误体多了 `error.details`（比如重名时已有平台的 id）。迁移 `006`（打卡事件表一并建好）。
- **本期、打卡、回本**：会员权益 tab 顶上多了「本期 | 全部」和「我 / 全家」（选择记在本机）。「本期」先列要处理的
  （快到期、应已自动续费、权益快过期），再按领取平台列出本期待领的（「优酷 · 3 项」，每行写来自哪张卡、本月领了几次、
  还剩几天、领取路径点一下复制），右边一个「领了」/「用了」一键打卡，snackbar 里能撤销；长按可以记多份、改日期、改价值、
  本期跳过；N 选 1 点选项打卡，这一期挑过了其余的置灰。年卡类权益打完卡顺手「在优酷建会员卡」。额度按每月、每年、会籍期、
  叠加上限现算（按会员本期起算时月末开通的卡不会漂），超额只提示不拦。会员行和详情有回本条：「已回本 62% · ¥55 / ¥88」
  对照「时间已过 70%」，详情里还有「还能再享」、打卡记录（可删、能撤销）和「续了一期」。首页多了一段「会员权益」，
  最多 3 条，权益快过期的直接点 ✓，该续费的点「续了」（到期前就续也不影响还在用的这一期，连点、两台设备各点一次都只续一期）；
  「知道了」只记在本机。
  接口 `/benefit-events`（打卡与撤销，带 `clientId` 幂等）、`POST /memberships/:id/renew`（续一期，可只关联已有流水）。
- **AI 智能导入（粘贴）**：把 88VIP、京东 PLUS 这类权益说明或订单详情粘进来，AI 拆成平台、会员卡、权益和买的东西，
  先给一份能逐条改的预览再落库。预览是一棵树（平台 → 卡 → 权益，外加「未归属」和「实物」），每一项标着新建 / 合并到「淘宝」/
  更新 3 项 / 可能重复 / 低置信 / 缺字段 / 依据未核实，点开能改（网页宽屏在右栏改，顶上高亮原文里的依据）；「领取平台」一栏
  集中确认每项权益去哪领、能并入已有的平台（名字记成别名）；长按多选能批量设领取平台、周期、价值、移到别的卡。已有的卡只补空的、
  到期日取更晚的，费用、额度不同只提示，库里有而这次没提到的只说「本次材料未提及」，永远不删。订单里的物品按预设算估值，
  同金额、前后三天只有一笔支出时默认关联那笔、不重复记账。导入后字段旁的「AI 推断」小点点一下能确认或去改。
  入口：物品页的「智能导入」、会员权益页的「+」和空状态、会员详情的「AI 补充权益」。每人每小时 20 次、同时 1 次；
  发给模型前手机号打码、卡号只留尾号。AI 渠道的「高级」里能填附加请求参数（比如关掉思考）和导入的输出上限。
  接口 `POST /asset-import/extract`（SSE）、`POST /asset-import/apply`（单事务、带 `clientId` 幂等）；迁移 `007`（`ai_imports`、物品的 `origin`）。
- **AI 智能导入（截图、撤销、续写）**：导入页多了「截图」：从相册选会员权益页或订单的截图（一次最多 6 张），长截图在手机上自动切成
  几片（每片高是宽的两倍、相邻重叠一点，合计最多 8 片）、压到长边 1568 以内再发，不想发的那片可以删掉；订单截图一样能导成物品、
  关联那笔流水。截图模式只列能看图的渠道：AI 渠道页多了「测看图」（发一张小图问颜色），测过的渠道名字旁边写「支持看图 / 看不了图」，
  换了模型要重测；看不了图的渠道答「我看不到图片，是红色吗」这类话不会被当成能看图。截图的依据块点开能全屏放大；截图识别的
  价格、日期、领取平台这些导进去标「AI 推断」，确认过再点掉。模型写到一半被截断、又已经收到 5 条以上时，自动带着「已收到」
  名单续写一次，还没写完（或者续写那次出错了）就在预览顶上说清楚、提示分段。核对到一半 App 被关掉或网页被刷新，回到导入页
  （网页刷新就在原页）能继续核对，改过的都在。导错了可以整批撤销：结果页的「撤销本次导入」，或者 7 天内在资产页右上角
  「最近的 AI 导入」里撤。新建的删掉（随物品记的支出一起删，关联的旧流水不动），更新过的改回原样；导入后又改过的、还挂着
  你后来加的东西的、卖出过的物品（连同买它时记的支出）留着，并说清楚是哪几项。
  接口 `POST /asset-import/extract` 支持 `kind: 'image'`，新增 `POST /asset-import/:id/undo`、`GET /asset-import/recent`、
  `POST /ai/providers/:id/test?vision=1`。
- **会员提醒（Android 通知）**：每天最多一条会员摘要通知，默认 09:00，「我的 › 会员提醒」里能改时间、能关掉。内容和首页、
  「要处理」同一套规则：续费、到期、试用结束、过期后待确认、本期没领完、权益快到期；只提醒自己的和全家共用的卡，点过「知道了」
  的不再提醒。预排未来 30 天，手机重启后照样弹；点通知直接到会员权益的「本期」。今天那条弹过之后再把时间改晚，不会再弹第二条；
  晚到的那条也不会因为打开家账被撤掉。通知权限和自动记账共用，在「会员提醒」页里允许，拒绝过两次后点「允许」直接打开系统的通知设置；
  小米手机要打开自启动，重启后才排得回来。用的是非精确闹钟，可能晚几分钟。网页版不能推送，只在页面里提醒；iOS 版这一期也不推送。
  退出登录先撤掉排着的会员提醒，提醒设置回到默认。真机验收步骤见 `docs/android-perk-reminders.md`。
- **扣费线索**：会员卡「更多」里能填扣费特征（商户关键词 + 金额范围，关键词用逗号、顿号或换行隔开，空格算在词里）。
  到期日前后流水里出现对得上的扣款（关键词在商户名或备注里）时，「要处理」里写「已看到 9/21 扣 ¥25.00 → 续到 10/20」，
  点「续上」就续一期，只关联那笔流水、不另记账，能撤销；不是这笔就点 ✕。一笔流水只能算在一张卡上。
  接口 `GET /memberships/charge-hints`；会员的 `payPattern`、`lastChargeTxId` 可写（`lastChargeTxId` 给的那笔已删除或不是
  确认过的支出时按空存，撤销「续上」不会因此失败）；`POST /memberships/:id/renew` 关联已被别的卡用过的流水时回 409 `charge_linked`。
- **网页版**：≥ 840 dp 时账单页换成宽表格，支持多选批量改类别、基金，批量删除，
  快捷键 `N`、`/`、`Esc`、`Delete`。导入和资产管理也能在网页上做。
- **自动记账 AI 兜底**：可以选本地或远程渠道；触发方式分自动、手动，按本地日志里的命中率校准。
- **修复**：
  - 网页版随机卡死：首页停在骨架屏、账单表格出不来、登录和导入一直转圈。原因是 service worker
    换版本时，Chrome 会把 fetch 的 keepalive 请求扣着不发；现在网页端请求一律不带 keepalive。
  - Docker 用 bind mount 挂一个还不存在的宿主目录时，容器报 `unable to open database file` 反复重启。
    现在容器以 root 入场，先把 `/data` 的属主改成 1000（可用 `PUID` / `PGID` 改）再降权运行，不用手动 chown。
  - 备份设置的「测试连接」直接用表单里现在填的值，不用先保存（`POST /backup/test` 可带
    `url`/`username`/`password`/`remoteDir`，不落库）。口令框留空时，只有地址和已保存的同源才沿用已保存的口令。
    地址停在威联通等 NAS 的根目录（PROPFIND 回 405）时，提示在地址后补上共享文件夹名。
  - 局域网、Tailscale 地址自动补全端口。
  - 基金页的新建按钮和右下角的 + 重叠。
- **文档**：README 精简并加了截图，`docs/ios.md` 也压短了。

## 0.1.0 — 2026-09-13

首个可用版本。

- **服务端**：Node 24 单进程，零第三方依赖，数据存 `node:sqlite`。模块自动装载，
  迁移向前兼容。
- **账本**：账户 × 基金两层模型；支出、收入、转账、拨款；类别、成员、预算。按 `clientId`
  幂等提交，用全局 `seq` 做增量同步。金额一律用整数「分」存。
- **App**：Flutter 3.32 一份代码出 Android、iOS、网页三端。窄屏用底部导航，≥ 840 dp
  换成导航轨加双栏。
- **自动记账**：通知文本依次经过归一化、查重、按来源抽字段、用户规则，再用朴素贝叶斯猜类别和基金，
  低于置信度阈值的进「待确认」。Android 监听通知后在通知栏给出「正确 / 修改… / 撤销」；
  iOS 走分享扩展、快捷指令、剪贴板，原生代码还没编译验证过。
- **备份**：`VACUUM INTO` 快照，gzip 压缩后可选再加密，上传到 WebDAV，按份数保留；
  恢复前要先过 sha256 和 `integrity_check` 校验。
- **AI**：兼容 `anthropic`、`openai` 两种协议，内置八个预设；有月报和问答。密钥加密存在服务端，
  按成员限流。
- **部署与 CI**：单镜像，可以从 GHCR 拉，也可以本地构建。`test.yml` 在 push 时自动跑，
  `build.yml` 手动触发发布（多架构镜像、APK、GitHub Release）。
- **安全**：口令用 scrypt 存；令牌可吊销；登录和初始化都有限流；首次初始化可以用 `SETUP_TOKEN`
  加锁。`secret.key` 是所有加密的根，要和数据库一起备份。
